// overlays.js - parking orbit lines, ascent arc, dashed transfer
export function createOverlays(scene){
  // placeholders for line meshes and markers
  return { transferLine: null, waitArc: null, ascent: null };
}
